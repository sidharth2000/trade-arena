server:
  port: 8085

spring:
  application:
    name: notification-service

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/notification_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: krithikaaie21@gmail.com
    password: ztftppfuxvqiuvon
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

trade-arena:
  mail:
    from: "Trade Arena <krithikaaie21@gmail.com>"
  jwt:
    secret: TradeArena@SecretKey#2025$JwtAuth!HS256Ready

app:
  sse:
    timeout: 300000

logging:
  level:
    com.tradearena.notificationservice: DEBUG
    org.springframework.mail: DEBUG
